---
layout: base.liquid
title: Portfolio - Hashim Warren
permalink: /portfolio/
description: "Explore marketing strategy projects by Hashim Warren, showcasing work for various clients."
---

<div class="container mt-5">
  <header class="text-center mb-5">
    <h1 class="display-4">Portfolio</h1>
    <p class="lead text-muted">A selection of projects that demonstrate my approach to marketing strategy and execution.</p>
  </header>

  <div class="row g-4">
    {%- if collections.project and collections.project.length > 0 -%}
      {%- for item in collections.project -%}
          <div class="col-md-4">
            <div class="card h-100 border-0 shadow-sm bg-white">
              {% if item.data.image %}
              <img src="{{ item.data.image }}" class="card-img-top card-img-top-fixed-height" alt="{{ item.data.imageAlt | default: 'Project image' }}">
              {% else %}
              <img src="https://placehold.co/600x400.png?text={{item.data.title | replace: ' ', '+'}}" class="card-img-top card-img-top-fixed-height" alt="Placeholder image for {{item.data.title}}">
              {% endif %}
              <div class="card-body text-center d-flex flex-column">
                {% if item.data.client %}<p class="text-muted text-uppercase small mb-2">{{ item.data.client }}</p>{% endif %}
                <h5 class="card-title text-uppercase">{{ item.data.title }}</h5>
                {% if item.data.description %}<p class="card-text text-start">{{ item.data.description }}</p>{% endif %}
                <a href="{{ item.url | url }}" class="btn btn-sm btn-outline-secondary mt-auto">View Details</a>
              </div>
            </div>
          </div>
        {%- endfor -%}
      {%- else -%}
        <div class="col-12">
          <p class="text-center">No projects to display yet. Please check back soon!</p>
          <!-- Placeholder cards if needed for layout testing when no projects exist -->
          <div class="row g-4 visually-hidden">
            {% for i in (1..3) %}
            <div class="col-md-4">
              <div class="card h-100 border-0 shadow-sm bg-white">
                <img src="https://placehold.co/600x400.png?text=Project+{{i}}" class="card-img-top card-img-top-fixed-height" alt="Placeholder Project Image {{i}}">
                <div class="card-body text-center d-flex flex-column">
                  <p class="text-muted text-uppercase small mb-2">CLIENT NAME {{i}}</p>
                  <h5 class="card-title text-uppercase">PROJECT TITLE {{i}}</h5>
                  <p class="card-text text-start">Brief description of the project and its outcomes. Highlighting key achievements.</p>
                  <a href="#" class="btn btn-sm btn-outline-secondary mt-auto">View Project</a>
                </div>
              </div>
            </div>
            {% endfor %}
          </div>
        </div>
    {%- endif -%}
  </div>
</div>
