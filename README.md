# Hashim Warren Site

Static HTML/CSS site for [hashimwarren.com](https://hashimwarren.com/).

There is no framework, backend, package manager, or build step. Vercel can serve the
repository root directly.
