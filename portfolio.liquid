---
layout: base.liquid
title: Portfolio - Hashim Warren
permalink: /portfolio/
description: "Explore marketing strategy projects by Hashim Warren, showcasing work for various clients."
---

<section class="py-5 bg-dark text-white">
  <div class="container py-5">
    <div class="row justify-content-center">
      <div class="col-lg-8 text-center">
        <h1 class="display-4 fw-bold mb-4">Portfolio</h1>
        <p class="lead fs-4 mb-0">A selection of projects that demonstrate my approach to marketing strategy and execution.</p>
      </div>
    </div>
  </div>
</section>

<section class="py-5">
  <div class="container py-4">
    {% if collections.project and collections.project.length > 0 %}
      <!-- Portfolio Filter (you can uncomment and implement if you want category filtering) -->
      <!-- 
      <div class="row justify-content-center mb-5">
        <div class="col-lg-8">
          <div class="d-flex justify-content-center flex-wrap gap-2">
            <button type="button" class="btn btn-outline-dark rounded-pill px-4 active" data-filter="all">All</button>
            <button type="button" class="btn btn-outline-dark rounded-pill px-4" data-filter="branding">Branding</button>
            <button type="button" class="btn btn-outline-dark rounded-pill px-4" data-filter="strategy">Strategy</button>
            <button type="button" class="btn btn-outline-dark rounded-pill px-4" data-filter="digital">Digital</button>
          </div>
        </div>
      </div>
      -->
      
      <!-- Portfolio Grid -->
      <div class="row g-4 g-lg-5">
        {%- for item in collections.project -%}
          <div class="col-md-6 col-lg-4">
            <div class="card h-100 border-0 bg-light rounded-4 overflow-hidden shadow-sm transition-hover">
              <div class="position-relative">
                {% if item.data.image %}
                <img src="{{ item.data.image }}" class="card-img-top" style="height: 240px; object-fit: cover;" alt="{{ item.data.imageAlt | default: 'Project image' }}">
                {% else %}
                <img src="https://placehold.co/800x600.png?text={{item.data.title | replace: ' ', '+'}}" class="card-img-top" style="height: 240px; object-fit: cover;" alt="Placeholder image for {{item.data.title}}">
                {% endif %}
                <div class="position-absolute top-0 end-0 p-3">
                  {% if item.data.featured %}<span class="badge bg-primary text-white rounded-pill px-3 py-2">Featured</span>{% endif %}
                </div>
              </div>
              <div class="card-body p-4">
                {% if item.data.client %}
                <div class="d-flex align-items-center mb-2">
                  <span class="badge bg-secondary bg-opacity-10 text-secondary px-3 py-2 fw-normal">{{ item.data.client }}</span>
                </div>
                {% endif %}
                <h5 class="fw-bold mb-3">{{ item.data.title }}</h5>
                {% if item.data.description %}
                <p class="text-secondary mb-4">{{ item.data.description }}</p>
                {% endif %}
                <a href="{{ item.url | url }}" class="stretched-link btn btn-sm btn-dark rounded-pill px-4">View Project</a>
              </div>
            </div>
          </div>
        {%- endfor -%}
      </div>
    {% else %}
      <div class="row justify-content-center">
        <div class="col-lg-8 text-center py-5">
          <div class="py-5">
            <h3 class="fw-light mb-4">No projects to display yet. Please check back soon!</h3>
            <p class="text-secondary">I'm working on adding my portfolio projects here.</p>
          </div>
        </div>
      </div>
    {% endif %}
  </div>
</section>

<section class="py-5 bg-dark text-white">
  <div class="container py-4">
    <div class="row justify-content-center text-center">
      <div class="col-lg-8">
        <h2 class="fw-bold mb-4">Ready to Work Together?</h2>
        <p class="lead mb-4">Let's discuss how my marketing strategy expertise can help your business grow.</p>
        <a href="/contact/" class="btn btn-light btn-lg rounded-pill px-5">Get in Touch</a>
      </div>
    </div>
  </div>
</section>
