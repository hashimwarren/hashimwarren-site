---
layout: base.liquid
title: Portfolio
permalink: /portfolio/index.html
---

<div class="container mt-4">
  <header class="py-5 text-center">
    <h1 class="fw-light">My Work</h1>
    <p class="lead text-muted">A selection of projects showcasing my marketing strategy expertise.</p>
  </header>

  <section class="py-3">
    <div class="row g-4">
      {%- if collections.project and collections.project.length > 0 -%}
        {%- for item in collections.project -%}
          <div class="col-md-4">
            <div class="card h-100">
              {% if item.data.image %}
                <img src="{{ item.data.image }}" class="card-img-top card-img-top-fixed-height" alt="{{ item.data.imageAlt | default: 'Project image' }}">
              {% else %}
                <img src="https://placehold.co/600x400.png?text={{ item.data.title | default: 'Project Image' | replace: ' ', '+' }}" class="card-img-top card-img-top-fixed-height" alt="Placeholder for {{ item.data.title }}">
              {% endif %}
              <div class="card-body text-center">
                {% if item.data.client %}
                  <p class="text-muted text-uppercase small mb-2">{{ item.data.client }}</p>
                {% endif %}
                <h5 class="card-title text-uppercase">{{ item.data.title | default: "Project Title" }}</h5>
                {% if item.data.description %}
                  <p class="card-text text-start">{{ item.data.description }}</p>
                {% endif %}
                <a href="{{ item.url | url }}" class="btn btn-sm btn-outline-secondary">View Project</a>
              </div>
            </div>
          </div>
        {%- endfor -%}
      {%- else -%}
        <div class="col">
          <p class="text-center text-muted">Portfolio projects will be listed here soon.</p>
          <!-- Placeholder cards if no collection exists or is empty -->
          <div class="row g-4">
            {% for i in (1..3) %}
            <div class="col-md-4">
              <div class="card h-100">
                <img src="https://placehold.co/600x400.png?text=Project+{{ i }}" class="card-img-top card-img-top-fixed-height" alt="Placeholder Project Image {{ i }}">
                <div class="card-body text-center">
                  <p class="text-muted text-uppercase small mb-2">CLIENT NAME {{ i }}</p>
                  <h5 class="card-title text-uppercase">Project Title {{ i }}</h5>
                  <p class="card-text text-start">Brief description of the project and its outcomes. Highlighting key achievements.</p>
                  <a href="#" class="btn btn-sm btn-outline-secondary">View Project</a>
                </div>
              </div>
            </div>
            {% endfor %}
          </div>
        </div>
      {%- endif -%}
    </div>
  </section>
</div>
