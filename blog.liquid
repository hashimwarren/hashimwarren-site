---
layout: base.liquid
title: Blog - Hashim Warren
permalink: /blog/
description: "Read the latest insights on marketing strategy, digital trends, and community building from Hashim Warren."
---

<div class="container mt-5">
  <header class="text-center mb-5">
    <h1 class="display-4">Blog</h1>
    <p class="lead text-muted">Insights on marketing strategy, digital trends, and community building.</p>
  </header>

  <div class="row g-4">
    {%- if collections.posts and collections.posts.length > 0 -%}
      {%- for post in collections.posts | reverse -%}
          <div class="col-md-4">
            <div class="card h-100 border-0 shadow-sm bg-white">
               {% if post.data.featuredImage %}
              <img src="{{ post.data.featuredImage }}" class="card-img-top card-img-top-fixed-height" alt="{{ post.data.imageAlt | default: 'Blog post image' }}">
              {% else %}
              <img src="https://placehold.co/600x400.png?text={{post.data.title | replace: ' ', '+'}}" class="card-img-top card-img-top-fixed-height" alt="Placeholder image for {{post.data.title}}">
              {% endif %}
              <div class="card-body text-center d-flex flex-column">
                {% if post.data.date %}<p class="text-muted text-uppercase small mb-2">{{ post.date | date: "%B %d, %Y" }}</p>{% endif %}
                <h5 class="card-title text-uppercase">{{ post.data.title }}</h5>
                {% if post.data.summary %}<p class="card-text text-start">{{ post.data.summary }}</p>{% endif %}
                <a href="{{ post.url | url }}" class="btn btn-sm btn-outline-secondary mt-auto">Read More</a>
              </div>
            </div>
          </div>
        {%- endfor -%}
      {%- else -%}
        <div class="col-12">
          <p class="text-center">No blog posts yet. Stay tuned!</p>
          <!-- Placeholder cards if needed for layout testing when no posts exist -->
          <div class="row g-4 visually-hidden">
            {% for i in (1..3) %}
            <div class="col-md-4">
              <div class="card h-100 border-0 shadow-sm bg-white">
                <img src="https://placehold.co/600x400.png?text=Blog+Post+{{i}}" class="card-img-top card-img-top-fixed-height" alt="Placeholder Blog Post Image {{i}}">
                <div class="card-body text-center d-flex flex-column">
                  <p class="text-muted text-uppercase small mb-2">MAY {{ 15 | minus: i }}, 2025</p>
                  <h5 class="card-title text-uppercase">POST TITLE {{i}}</h5>
                  <p class="card-text text-start">This is a summary of the blog post, providing a natural lead-in to the full content.</p>
                  <a href="#" class="btn btn-sm btn-outline-secondary mt-auto">Read More</a>
                </div>
              </div>
            </div>
            {% endfor %}
          </div>
        </div>
    {%- endif -%}
  </div>
</div>
